apiVersion: 1

providers:
  - name: Prior Authorization
    orgId: 1
    folder: Prior Authorization
    folderUid: prior-authorization
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
